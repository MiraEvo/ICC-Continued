## 移除无用代码计划

经过全面分析，发现以下可以移除的无用代码：

### 1. 空壳类（向后兼容别名）- Resources/Settings.cs
**位置**: [Resources/Settings.cs:L229-L269](file:///c:/Users/SakuraStar/Desktop/ICC-Continued/InkCanvasForClass/Resources/Settings.cs#L229-L269)

**可以移除的9个空壳类**：
- `Snapshot : SnapshotSettings`
- `Storage : StorageSettings`
- `Canvas : CanvasSettings`
- `Gesture : GestureSettings`
- `Startup : StartupSettings`
- `Appearance : AppearanceSettings`
- `Automation : AutomationSettings`
- `Advanced : AdvancedSettings`
- `InkToShape : InkToShapeSettings`

**理由**：这些类只是继承了新的 Settings 类，没有添加任何新功能。经过全局搜索，代码中没有直接使用这些别名类（如 `new Snapshot()`），而是直接使用 `Settings.Snapshot` 等形式访问属性。

### 2. 空的 InkCanvas 文件
**位置**: [MainWindow.Partials/UI/MainWindow.InkCanvas.cs](file:///c:/Users/SakuraStar/Desktop/ICC-Continued/InkCanvasForClass/MainWindow.Partials/UI/MainWindow.InkCanvas.cs)

**操作**: 删除整个文件

**理由**: 该文件现在只包含注释说明（29行），实际的 `IccStroke` 和 `IccInkCanvas` 类已经迁移到 `MainWindow.InkCanvas.Refactored.cs` 中。

### 3. 需要保留的代码
- **RequiresUnmanagedCodeAttribute**: 被项目中的54处代码使用，不能删除
- **TODO注释**: 在 HotkeyService.cs 中的 TODO 是有效的待办事项

## 实施步骤
1. 删除 `MainWindow.Partials/UI/MainWindow.InkCanvas.cs` 文件
2. 删除 `Resources/Settings.cs` 中的9个空壳类定义（229-269行）
3. 构建项目验证没有编译错误