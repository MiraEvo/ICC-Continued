<Window x:Class="Ink_Canvas.CountdownTimerWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
        xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
        xmlns:local="clr-namespace:Ink_Canvas"
        xmlns:ui="http://schemas.inkore.net/lib/ui/wpf/modern"
        xmlns:processBars="clr-namespace:Ink_Canvas.ProcessBars"
        mc:Ignorable="d" Topmost="True" WindowStartupLocation="CenterScreen" WindowStyle="None" AllowsTransparency="True"
        ui:ThemeManager.RequestedTheme="Light" ResizeMode="CanResize" WindowState="Normal"
        Title="倒计时 - Ink Canvas 画板" Height="700" Width="1100" FontFamily="Microsoft YaHei UI"
        Loaded="Window_Loaded" Closing="Window_Closing" MouseMove="WindowDragMove">
    <Window.Resources>
        <ResourceDictionary>
            <ResourceDictionary.MergedDictionaries>
                <ResourceDictionary Source="pack://application:,,,/InkCanvasForClass;component/Resources/Converters/ConvertersDictionary.xaml"/>
            </ResourceDictionary.MergedDictionaries>
        </ResourceDictionary>
    </Window.Resources>
    <Grid>
        <Grid x:Name="BigViewController">
            <Grid.ColumnDefinitions>
                <ColumnDefinition Width="1.8*"/>
                <ColumnDefinition Width="1*"/>
            </Grid.ColumnDefinitions>
            <Grid Background="#FBFBFD">
                <Grid.RowDefinitions>
                    <RowDefinition/>
                    <RowDefinition Height="60"/>
                </Grid.RowDefinitions>
                <processBars:CycleProcessBar x:Name="ProcessBarTime" Visibility="{Binding IsSettingMode, Converter={StaticResource InverseBoolToVisibilityConverter}}" CurrentValue="{Binding ProgressValue}" IsPaused="{Binding IsProgressPaused}"/>
                <Grid Visibility="{Binding IsSettingMode, Converter={StaticResource BoolToVisibilityConverter}}" x:Name="GridAdjustHour">
                    <Grid.RowDefinitions>
                        <RowDefinition/>
                        <RowDefinition/>
                        <RowDefinition/>
                    </Grid.RowDefinitions>
                    <Grid.ColumnDefinitions>
                        <ColumnDefinition/>
                        <ColumnDefinition/>
                        <ColumnDefinition/>
                        <ColumnDefinition/>
                    </Grid.ColumnDefinitions>
                    <Button Margin="10" Content="+1" FontSize="50" Click="ButtonHourPlus1_Click"/>
                    <Button Margin="10" Grid.Column="1" Content="+1" FontSize="50" Click="ButtonMinutePlus1_Click"/>
                    <Button Margin="10" Grid.Column="2" Content="+5" FontSize="50" Click="ButtonSecondPlus5_Click"/>
                    <Button Margin="10" Grid.Column="3" Content="+1" FontSize="50" Click="ButtonSecondPlus1_Click"/>
                    <Button Margin="10" Grid.Row="2" Content="-1" FontSize="50" Click="ButtonHourMinus1_Click"/>
                    <Button Margin="10" Grid.Row="2" Grid.Column="1" Content="-1" FontSize="50" Click="ButtonMinuteMinus1_Click"/>
                    <Button Margin="10" Grid.Row="2" Grid.Column="2" Content="-5" FontSize="50" Click="ButtonSecondMinus5_Click"/>
                    <Button Margin="10" Grid.Row="2" Grid.Column="3" Content="-1" FontSize="50" Click="ButtonSecondMinus1_Click"/>
                </Grid>
                <Viewbox Grid.Row="1" HorizontalAlignment="Center" VerticalAlignment="Center">
                    <TextBlock Margin="10" FontSize="30" Foreground="{Binding TimeForeground}" MouseUp="GridTime_MouseUp">
                        <Run Text="{Binding HourText}"/><Run Text=":"/><Run Text="{Binding MinuteText}"/><Run Text=":"/><Run Text="{Binding SecondText}"/>
                    </TextBlock>
                </Viewbox>
            </Grid>
            <Grid Grid.Column="1">
                <Grid.RowDefinitions>
                    <RowDefinition Height="60"/>
                    <RowDefinition/>
                    <RowDefinition Height="60"/>
                </Grid.RowDefinitions>
                <ui:SimpleStackPanel Orientation="Horizontal" Spacing="10" HorizontalAlignment="Right" VerticalAlignment="Center" Margin="10,0">
                    <Border x:Name="BtnMinimal" MouseUp="BtnMinimal_MouseUp" Background="Transparent" Height="40" Width="40" CornerRadius="5">
                        <Viewbox Margin="10">
                            <ui:FontIcon Glyph="&#xE7F4;" Foreground="#5B5D5F"/>
                        </Viewbox>
                    </Border>
                    <Border x:Name="BtnFullscreen" MouseUp="BtnFullscreen_MouseUp" Background="Transparent" Height="40" Width="40" CornerRadius="5">
                        <Viewbox Margin="10">
                            <ui:FontIcon x:Name="SymbolIconFullscreen" Glyph="{Binding FullscreenButtonIcon}" Foreground="#5B5D5F"/>
                        </Viewbox>
                    </Border>
                    <Border x:Name="BtnClose" MouseUp="BtnClose_MouseUp" Background="Transparent" Height="40" Width="40" CornerRadius="5">
                        <Viewbox Margin="10">
                            <ui:FontIcon Glyph="&#xE894;" Foreground="#5B5D5F"/>
                        </Viewbox>
                    </Border>
                </ui:SimpleStackPanel>
                <ui:SimpleStackPanel Grid.Row="1" Orientation="Vertical" Spacing="10" HorizontalAlignment="Center" VerticalAlignment="Center">
                    <Border x:Name="BtnStart" MouseUp="BtnStart_MouseUp" Background="#0066BF" Height="70" Width="200" CornerRadius="35" Opacity="{Binding IsStartButtonEnabled, Converter={StaticResource BooleanToOpacityConverter}}">
                        <Viewbox Margin="20">
                            <ui:FontIcon x:Name="SymbolIconStart" Glyph="{Binding StartButtonIcon}" Foreground="White"/>
                        </Viewbox>
                    </Border>
                    <Border x:Name="BtnStartCover" MouseUp="BtnStart_MouseUp" Background="Transparent" Height="70" Width="200" CornerRadius="35" Visibility="{Binding IsStartButtonEnabled, Converter={StaticResource InverseBoolToVisibilityConverter}}"/>
                    <Border x:Name="BtnReset" MouseUp="BtnReset_MouseUp" Background="#FBFBFD" Height="70" Width="200" CornerRadius="35" Opacity="{Binding IsResetButtonEnabled, Converter={StaticResource BooleanToOpacityConverter}}">
                        <Border.Effect>
                            <DropShadowEffect Direction="0" ShadowDepth="0" Opacity="0.15" BlurRadius="3"/>
                        </Border.Effect>
                        <Viewbox Margin="20">
                            <ui:FontIcon Glyph="&#xE72C;" Foreground="Black"/>
                        </Viewbox>
                    </Border>
                    <Border x:Name="BtnResetCover" MouseUp="BtnReset_MouseUp" Background="Transparent" Height="70" Width="200" CornerRadius="35" Visibility="{Binding IsResetButtonEnabled, Converter={StaticResource InverseBoolToVisibilityConverter}}"/>
                </ui:SimpleStackPanel>
                <Border x:Name="BorderStopTime" Grid.Row="2" Background="#FBFBFD" Height="40" CornerRadius="20" Margin="10,0" Visibility="{Binding IsStopTimeVisible, Converter={StaticResource BoolToVisibilityConverter}}">
                    <Border.Effect>
                        <DropShadowEffect Direction="0" ShadowDepth="0" Opacity="0.15" BlurRadius="3"/>
                    </Border.Effect>
                    <TextBlock x:Name="TextBlockStopTime" Text="{Binding StopTimeText}" VerticalAlignment="Center" HorizontalAlignment="Center" FontSize="20"/>
                </Border>
            </Grid>
        </Grid>
        <TextBlock x:Name="TbCurrentTime" Text="{Binding CurrentTimeText}" Visibility="Collapsed" VerticalAlignment="Center" HorizontalAlignment="Center" FontSize="100" Foreground="White"/>
    </Grid>
</Window>
