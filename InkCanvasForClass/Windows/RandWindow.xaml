<Window x:Class="Ink_Canvas.RandWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
        xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
        xmlns:ui="http://schemas.lepo.co/wpfui/2022/xaml"
        xmlns:local="clr-namespace:Ink_Canvas" FontFamily="Microsoft YaHei UI"
        Topmost="True" Background="Transparent"
        mc:Ignorable="d" WindowStyle="None" AllowsTransparency="True"
        WindowStartupLocation="CenterScreen"
        Title="Ink Canvas 抽奖" Height="500" Width="900">
    <Border Background="#F0F3F9" CornerRadius="10" BorderThickness="1" BorderBrush="#0066BF" Margin="0" ClipToBounds="True">
        <Canvas>
            <Image Source="/Resources/hatsune-miku1.png" Width="300" Canvas.Bottom="-140" Canvas.Left="-48" Canvas.Top="304"></Image>
            <Grid Canvas.Left="0" Canvas.Right="0" Canvas.Top="0" Canvas.Bottom="0" Width="900" Height="309" HorizontalAlignment="Center" VerticalAlignment="Top">
                <Grid.ColumnDefinitions>
                    <ColumnDefinition Width="1.8*"/>
                    <ColumnDefinition Width="1*"/>
                </Grid.ColumnDefinitions>
                <Viewbox HorizontalAlignment="Center" Margin="20,0">
                    <StackPanel Orientation="Horizontal">
                        <TextBlock FontSize="130" Text="{Binding OutputText}" TextAlignment="Center" HorizontalAlignment="Center" VerticalAlignment="Center" FontWeight="Bold" Margin="25,0"/>
                        <TextBlock FontSize="130" Visibility="{Binding IsOutput2Visible, Converter={StaticResource BoolToVisibilityConverter}}" Text="{Binding OutputText2}" TextAlignment="Center" HorizontalAlignment="Center" VerticalAlignment="Center" FontWeight="Bold" Margin="25,0"/>
                        <TextBlock FontSize="130" Visibility="{Binding IsOutput3Visible, Converter={StaticResource BoolToVisibilityConverter}}" Text="{Binding OutputText3}" TextAlignment="Center" HorizontalAlignment="Center" VerticalAlignment="Center" FontWeight="Bold" Margin="25,0"/>
                    </StackPanel>
                </Viewbox>
                <StackPanel Grid.Column="1" HorizontalAlignment="Left" VerticalAlignment="Center" Opacity="{Binding ControlPanelOpacity}" IsEnabled="{Binding IsControlPanelEnabled}" Name="PeopleControlPane" Width="256">
                    <StackPanel Orientation="Horizontal" HorizontalAlignment="Center">
                        <Border x:Name="BorderBtnMinus" MouseUp="BorderBtnMinus_MouseUp" Background="#FBFBFD" Height="70" Width="70" CornerRadius="100" Opacity="{Binding DecrementCountCommand.CanExecute, Converter={StaticResource BooleanToOpacityConverter}}">
                            <Border.Effect>
                                <DropShadowEffect Direction="0" ShadowDepth="0" Opacity="0.15" BlurRadius="3"/>
                            </Border.Effect>
                            <Viewbox Margin="20">
                                <ui:SymbolIcon Symbol="Subtract24" Foreground="Black"/>
                            </Viewbox>
                        </Border>
                        <TextBlock Text="{Binding TotalCount}" FontFamily="Consolas" FontSize="55" Margin="10" Width="80" TextAlignment="Center" VerticalAlignment="Center"/>
                        <Border x:Name="BorderBtnAdd" MouseUp="BorderBtnAdd_MouseUp" Background="#FBFBFD" Height="70" Width="70" CornerRadius="100" Opacity="{Binding IncrementCountCommand.CanExecute, Converter={StaticResource BooleanToOpacityConverter}}">
                            <Border.Effect>
                                <DropShadowEffect Direction="0" ShadowDepth="0" Opacity="0.15" BlurRadius="3"/>
                            </Border.Effect>
                            <Viewbox Margin="20">
                                <ui:SymbolIcon Symbol="Add24" Foreground="Black"/>
                            </Viewbox>
                        </Border>
                    </StackPanel>
                    <StackPanel Visibility="Collapsed" Margin="0,8,0,0" Orientation="Horizontal" HorizontalAlignment="Center">
                        <CheckBox x:Name="NoHotStudents" MinWidth="0"
                                  Content="不抽热门人选" IsThreeState="True"
                                  HorizontalAlignment="Center" />
                        <CheckBox x:Name="NoShengPiZi" MinWidth="0"
                                  Content="避开生僻字" IsThreeState="True"
                                  HorizontalAlignment="Center" />
                    </StackPanel>
                    <StackPanel Visibility="Collapsed" Margin="0,0,0,8" Orientation="Horizontal" HorizontalAlignment="Center">
                        <ComboBox Name="ComboBoxRandMode"
                                  MinWidth="0"
                                  IsEditable="False"
                                  SelectedIndex="0"
                                  IsReadOnly="True">
                            <ComboBoxItem>全都抽</ComboBoxItem>
                            <ComboBoxItem>只抽男</ComboBoxItem>
                            <ComboBoxItem>只抽女</ComboBoxItem>
                        </ComboBox>
                    </StackPanel>
                    <Border x:Name="BorderBtnRand" MouseUp="BorderBtnRand_MouseUp" Background="#0066BF" Height="70" Width="200" CornerRadius="35" Opacity="{Binding StartRandomSelectionCommand.CanExecute, Converter={StaticResource BooleanToOpacityConverter}}">
                        <StackPanel Margin="3,0" Orientation="Horizontal" HorizontalAlignment="Center">
                            <Viewbox Margin="0,20">
                                <ui:SymbolIcon Symbol="{Binding StartButtonIcon}" Foreground="White"/>
                            </Viewbox>
                            <TextBlock Text="开抽" Foreground="White" FontSize="32" Margin="10,0,4,0" VerticalAlignment="Center"/>
                        </StackPanel>
                    </Border>
                </StackPanel>
            </Grid>
            <Border UseLayoutRounding="True" Canvas.Bottom="8" Canvas.Right="8" x:Name="BorderBtnHelp" MouseUp="BorderBtnHelp_MouseUp" Background="#FBFBFD" Grid.Column="1" Margin="10,10,60,10" Height="40" VerticalAlignment="Bottom" HorizontalAlignment="Right" CornerRadius="20" Visibility="{Binding IsHelpButtonVisible, Converter={StaticResource BoolToVisibilityConverter}}">
                <Border.Effect>
                    <DropShadowEffect Direction="0" ShadowDepth="0" Opacity="0.15" BlurRadius="3"/>
                </Border.Effect>
                <StackPanel Orientation="Horizontal" HorizontalAlignment="Center">
                    <Viewbox Margin="15,12">
                        <ui:SymbolIcon Symbol="People24" Foreground="Black"/>
                    </Viewbox>
                    <TextBlock Margin="5,12,15,12" Text="{Binding PeopleCountText}" FontSize="16" HorizontalAlignment="Center" VerticalAlignment="Center"/>
                </StackPanel>
            </Border>
            <Border UseLayoutRounding="True" Canvas.Bottom="8" Canvas.Right="8" x:Name="BtnClose" MouseUp="BtnClose_MouseUp" HorizontalAlignment="Right" VerticalAlignment="{Binding ElementName=BorderBtnHelp, Path=VerticalAlignment}" Margin="10" Grid.Column="1" Background="#E32A34" Height="40" Width="40" CornerRadius="100">
                <Border.Effect>
                    <DropShadowEffect Direction="0" ShadowDepth="0" Opacity="0.1" BlurRadius="3"/>
                </Border.Effect>
                <Viewbox Margin="14">
                    <ui:SymbolIcon Symbol="Dismiss24" Foreground="White"/>
                </Viewbox>
            </Border>
        </Canvas>

    </Border>
</Window>
