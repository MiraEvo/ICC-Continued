<UserControl x:Class="Ink_Canvas.Views.Settings.Pages.GestureSettingsPage"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
             xmlns:ui="http://schemas.inkore.net/lib/ui/wpf/modern"
             xmlns:local="clr-namespace:Ink_Canvas.Views.Settings.Pages"
             mc:Ignorable="d"
             d:DesignHeight="1000" d:DesignWidth="600"
             Background="Transparent"
             Foreground="#FAFAFA"
             ui:ThemeManager.RequestedTheme="Dark">

    <UserControl.Resources>
        <ResourceDictionary>
            <ResourceDictionary.MergedDictionaries>
                <ResourceDictionary Source="SettingsPageStyles.xaml"/>
            </ResourceDictionary.MergedDictionaries>
        </ResourceDictionary>
    </UserControl.Resources>

    <ScrollViewer VerticalScrollBarVisibility="Auto" PanningMode="VerticalOnly">
        <StackPanel Margin="0,0,12,24">
        <TextBlock Text="手势" Style="{StaticResource PageTitleStyle}"/>
        <TextBlock Text="触控与鼠标手势行为设置" Style="{StaticResource PageSubtitleStyle}"/>

        <!-- ==================== 触摸手势 ==================== -->

        <!-- 进退白板模式自动开关双指移动功能 -->
        <Border Style="{StaticResource SettingsCardStyle}">
            <StackPanel>
                <Grid>
                    <Grid.ColumnDefinitions>
                        <ColumnDefinition Width="*"/>
                        <ColumnDefinition Width="Auto"/>
                    </Grid.ColumnDefinitions>
                    <StackPanel Grid.Column="0" VerticalAlignment="Center">
                        <TextBlock Text="进退白板模式自动开关双指移动功能" Style="{StaticResource SettingsTitleStyle}"/>
                    </StackPanel>
                    <ui:ToggleSwitch Grid.Column="1" VerticalAlignment="Center"
                                     IsOn="{Binding Gesture.AutoSwitchTwoFingerGesture, Mode=TwoWay}"/>
                </Grid>
                <TextBlock Text="开启后退出画板模式时自动关闭双指移动手势，进入白板模式时自动开启双指移动手势"
                           Style="{StaticResource HintTextStyle}"/>
            </StackPanel>
        </Border>

        <!-- 允许双指旋转与缩放选中的墨迹 -->
        <Border Style="{StaticResource SettingsCardStyle}">
            <StackPanel>
                <Grid>
                    <Grid.ColumnDefinitions>
                        <ColumnDefinition Width="*"/>
                        <ColumnDefinition Width="Auto"/>
                    </Grid.ColumnDefinitions>
                    <StackPanel Grid.Column="0" VerticalAlignment="Center">
                        <TextBlock Text="允许双指旋转与缩放选中的墨迹" Style="{StaticResource SettingsTitleStyle}"/>
                    </StackPanel>
                    <ui:ToggleSwitch Grid.Column="1" VerticalAlignment="Center"
                                     IsOn="{Binding Gesture.IsEnableTwoFingerRotationOnSelection, Mode=TwoWay}"/>
                </Grid>
                <TextBlock Text="允许选中墨迹后对墨迹进行双指或多指缩放操作（此设置不受&quot;允许双指旋转&quot;设置的影响）"
                           Style="{StaticResource HintTextStyle}"/>
            </StackPanel>
        </Border>

        <!-- 禁用手势橡皮 -->
        <Border Style="{StaticResource SettingsCardStyle}">
            <StackPanel>
                <Grid>
                    <Grid.ColumnDefinitions>
                        <ColumnDefinition Width="*"/>
                        <ColumnDefinition Width="Auto"/>
                    </Grid.ColumnDefinitions>
                    <StackPanel Grid.Column="0" VerticalAlignment="Center" Orientation="Horizontal">
                        <TextBlock Text="禁用手势橡皮" Style="{StaticResource SettingsTitleStyle}"/>
                        <Border Style="{StaticResource NewFeatureTagStyle}">
                            <TextBlock Text="✦ 新功能" Style="{StaticResource NewFeatureTagTextStyle}"/>
                        </Border>
                    </StackPanel>
                    <ui:ToggleSwitch Grid.Column="1" VerticalAlignment="Center"
                                     IsOn="{Binding Gesture.DisableGestureEraser, Mode=TwoWay}"/>
                </Grid>
                <TextBlock Text="禁用Ink Canvas祖传的手势橡皮，禁用后，高级选项中的&quot;特殊屏幕模式&quot;、&quot;BoundsWidth&quot;、&quot;橡皮擦触摸大小倍数&quot;、&quot;四边红外模式&quot;等选项将被禁用，同时也不能使用手指和手掌擦除墨迹。"
                           Style="{StaticResource HintTextStyle}"/>
            </StackPanel>
        </Border>

        <!-- 多指书写功能 -->
        <Border Style="{StaticResource SettingsCardStyle}">
            <Grid>
                <Grid.ColumnDefinitions>
                    <ColumnDefinition Width="Auto"/>
                    <ColumnDefinition Width="*"/>
                    <ColumnDefinition Width="Auto"/>
                </Grid.ColumnDefinitions>
                <TextBlock Grid.Column="0" Text="多指书写功能" Style="{StaticResource SettingsTitleStyle}"
                           VerticalAlignment="Center" Margin="0,0,16,0"/>
                <ComboBox Grid.Column="2" Width="140"
                          SelectedIndex="{Binding Gesture.DefaultMultiPointHandWritingMode, Mode=TwoWay}">
                    <ComboBoxItem Content="禁用"/>
                    <ComboBoxItem Content="只开启触摸"/>
                    <ComboBoxItem Content="保持上次选择"/>
                </ComboBox>
            </Grid>
        </Border>

        <!-- 触摸书写时隐藏鼠标指针 -->
        <Border Style="{StaticResource SettingsCardStyle}">
            <StackPanel>
                <Grid>
                    <Grid.ColumnDefinitions>
                        <ColumnDefinition Width="Auto"/>
                        <ColumnDefinition Width="*"/>
                        <ColumnDefinition Width="Auto"/>
                        <ColumnDefinition Width="Auto"/>
                    </Grid.ColumnDefinitions>
                    <StackPanel Grid.Column="0" VerticalAlignment="Center" Orientation="Horizontal" Margin="0,0,16,0">
                        <TextBlock Text="触摸书写时隐藏鼠标指针" Style="{StaticResource SettingsTitleStyle}"/>
                        <Border Style="{StaticResource NewFeatureTagStyle}">
                            <TextBlock Text="✦ 新功能" Style="{StaticResource NewFeatureTagTextStyle}"/>
                        </Border>
                    </StackPanel>
                    <ComboBox Grid.Column="2" Width="140" Margin="0,0,16,0"
                              SelectedIndex="{Binding Gesture.HideCursorMode, Mode=TwoWay}">
                        <ComboBoxItem Content="每次启动都开启"/>
                        <ComboBoxItem Content="每次启动都关闭"/>
                    </ComboBox>
                    <ui:ToggleSwitch Grid.Column="3" VerticalAlignment="Center"
                                     IsOn="{Binding Gesture.HideCursorWhenUsingTouchDevice, Mode=TwoWay}"/>
                </Grid>
                <TextBlock Text="会在使用触摸屏时隐藏鼠标指针，这样就可以去掉触摸指针。若出现问题，请尝试快捷键&quot;Alt + C&quot;来强制显示鼠标指针并关闭该功能。"
                           Style="{StaticResource HintTextStyle}"/>
            </StackPanel>
        </Border>

        <!-- ==================== 鼠标手势 ==================== -->
        <TextBlock Text="鼠标手势" Style="{StaticResource SettingsGroupTitleStyle}"/>

        <!-- 鼠标手势总开关 -->
        <Border Style="{StaticResource SettingsCardStyle}">
            <StackPanel>
                <Grid>
                    <Grid.ColumnDefinitions>
                        <ColumnDefinition Width="*"/>
                        <ColumnDefinition Width="Auto"/>
                    </Grid.ColumnDefinitions>
                    <StackPanel Grid.Column="0" VerticalAlignment="Center" Orientation="Horizontal">
                        <TextBlock Text="鼠标手势总开关" Style="{StaticResource SettingsTitleStyle}"/>
                        <Border Style="{StaticResource NewFeatureTagStyle}">
                            <TextBlock Text="✦ 新功能" Style="{StaticResource NewFeatureTagTextStyle}"/>
                        </Border>
                    </StackPanel>
                    <ui:ToggleSwitch Grid.Column="1" VerticalAlignment="Center"
                                     IsOn="{Binding Gesture.EnableMouseGesture, Mode=TwoWay}"/>
                </Grid>
                <TextBlock Text="鼠标手势可以帮助您在使用鼠标进行批注书写时更加方便"
                           Style="{StaticResource HintTextStyle}"/>
            </StackPanel>
        </Border>

        <!-- 按住鼠标右键移动画布 -->
        <Border Style="{StaticResource SettingsCardStyle}">
            <Grid>
                <Grid.ColumnDefinitions>
                    <ColumnDefinition Width="Auto"/>
                    <ColumnDefinition Width="*"/>
                    <ColumnDefinition Width="Auto"/>
                </Grid.ColumnDefinitions>
                <ui:FontIcon Grid.Column="0" Glyph="&#xE72E;" Style="{StaticResource SettingsIconStyle}"/>
                <StackPanel Grid.Column="1" VerticalAlignment="Center" Orientation="Horizontal">
                    <TextBlock Text="按住鼠标右键移动画布" Style="{StaticResource SettingsTitleStyle}"/>
                    <Border Style="{StaticResource NewFeatureTagStyle}">
                        <TextBlock Text="✦ 新功能" Style="{StaticResource NewFeatureTagTextStyle}"/>
                    </Border>
                </StackPanel>
                <ui:ToggleSwitch Grid.Column="2" VerticalAlignment="Center"
                                 IsOn="{Binding Gesture.EnableMouseRightBtnGesture, Mode=TwoWay}"/>
            </Grid>
        </Border>

        <!-- 滑动鼠标滚轮 -->
        <Border Style="{StaticResource SettingsCardStyle}">
            <Grid>
                <Grid.ColumnDefinitions>
                    <ColumnDefinition Width="Auto"/>
                    <ColumnDefinition Width="Auto"/>
                    <ColumnDefinition Width="*"/>
                    <ColumnDefinition Width="Auto"/>
                    <ColumnDefinition Width="Auto"/>
                </Grid.ColumnDefinitions>
                <ui:FontIcon Grid.Column="0" Glyph="&#xE7AD;" Style="{StaticResource SettingsIconStyle}"/>
                <StackPanel Grid.Column="1" VerticalAlignment="Center" Orientation="Horizontal" Margin="0,0,16,0">
                    <TextBlock Text="滑动鼠标滚轮" Style="{StaticResource SettingsTitleStyle}"/>
                    <Border Style="{StaticResource NewFeatureTagStyle}">
                        <TextBlock Text="✦ 新功能" Style="{StaticResource NewFeatureTagTextStyle}"/>
                    </Border>
                </StackPanel>
                <ComboBox Grid.Column="3" Width="120" Margin="0,0,16,0"
                          SelectedIndex="{Binding Gesture.MouseWheelAction, Mode=TwoWay}">
                    <ComboBoxItem Content="缩放画布"/>
                    <ComboBoxItem Content="调整笔粗细"/>
                </ComboBox>
                <ui:ToggleSwitch Grid.Column="4" VerticalAlignment="Center"
                                 IsOn="{Binding Gesture.EnableMouseWheelGesture, Mode=TwoWay}"/>
            </Grid>
        </Border>

        <!-- 鼠标滚轮正方向 -->
        <Border Style="{StaticResource SettingsCardStyle}">
            <Grid>
                <Grid.ColumnDefinitions>
                    <ColumnDefinition Width="Auto"/>
                    <ColumnDefinition Width="*"/>
                    <ColumnDefinition Width="Auto"/>
                </Grid.ColumnDefinitions>
                <TextBlock Grid.Column="0" Text="鼠标滚轮正方向" Style="{StaticResource SettingsTitleStyle}"
                           VerticalAlignment="Center" Margin="0,0,16,0"/>
                <ComboBox Grid.Column="2" Width="120"
                          SelectedIndex="{Binding Gesture.MouseWheelDirection, Mode=TwoWay}">
                    <ComboBoxItem Content="滚轮向上"/>
                    <ComboBoxItem Content="滚轮向下"/>
                </ComboBox>
            </Grid>
        </Border>

        <!-- 底部间距 -->
        <Border Height="40"/>
    </StackPanel>
    </ScrollViewer>
</UserControl>
