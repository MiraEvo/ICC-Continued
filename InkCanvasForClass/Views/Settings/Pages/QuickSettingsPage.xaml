<UserControl x:Class="Ink_Canvas.Views.Settings.Pages.QuickSettingsPage"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
             xmlns:ui="http://schemas.inkore.net/lib/ui/wpf/modern"
             xmlns:local="clr-namespace:Ink_Canvas.Views.Settings.Pages"
             mc:Ignorable="d"
             d:DesignHeight="800" d:DesignWidth="600"
             Background="Transparent"
             Foreground="#FAFAFA"
             ui:ThemeManager.RequestedTheme="Dark">

    <UserControl.Resources>
        <ResourceDictionary>
            <ResourceDictionary.MergedDictionaries>
                <ResourceDictionary Source="SettingsPageStyles.xaml"/>
            </ResourceDictionary.MergedDictionaries>
        </ResourceDictionary>
    </UserControl.Resources>

    <StackPanel>
        <!-- 页面标题 -->
        <TextBlock Text="快速设置" Style="{StaticResource PageTitleStyle}"/>
        <TextBlock Text="常用设置项的快速访问入口" Style="{StaticResource PageSubtitleStyle}"/>

        <!-- 启动设置分组 -->
        <TextBlock Text="启动" Style="{StaticResource SettingsGroupTitleStyle}" Margin="0,8,0,12"/>

        <!-- 开机时运行 -->
        <Border Style="{StaticResource SettingsCardStyle}">
            <Grid>
                <Grid.ColumnDefinitions>
                    <ColumnDefinition Width="Auto"/>
                    <ColumnDefinition Width="*"/>
                    <ColumnDefinition Width="Auto"/>
                </Grid.ColumnDefinitions>

                <!-- 图标容器 -->
                <Border Style="{StaticResource SettingsIconContainerStyle}">
                    <ui:FontIcon Glyph="&#xE7E8;" FontSize="18"
                                 Foreground="{StaticResource AccentBrush}"
                                 HorizontalAlignment="Center" VerticalAlignment="Center"/>
                </Border>

                <StackPanel Grid.Column="1" VerticalAlignment="Center" Margin="0,0,16,0">
                    <TextBlock Text="开机时运行" Style="{StaticResource SettingsTitleStyle}"/>
                    <TextBlock Text="系统启动时自动运行此应用程序" Style="{StaticResource SettingsDescriptionStyle}"/>
                </StackPanel>
                <ui:ToggleSwitch x:Name="ToggleRunAtStartup" Grid.Column="2" VerticalAlignment="Center"
                                 Toggled="ToggleRunAtStartup_Toggled"/>
            </Grid>
        </Border>

        <!-- 开机运行后收纳到侧边栏 -->
        <Border Style="{StaticResource SettingsCardStyle}">
            <Grid>
                <Grid.ColumnDefinitions>
                    <ColumnDefinition Width="Auto"/>
                    <ColumnDefinition Width="*"/>
                    <ColumnDefinition Width="Auto"/>
                </Grid.ColumnDefinitions>

                <!-- 图标容器 -->
                <Border Style="{StaticResource SettingsIconContainerStyle}">
                    <ui:FontIcon Glyph="&#xE744;" FontSize="18"
                                 Foreground="{StaticResource AccentBrush}"
                                 HorizontalAlignment="Center" VerticalAlignment="Center"/>
                </Border>

                <StackPanel Grid.Column="1" VerticalAlignment="Center" Margin="0,0,16,0">
                    <TextBlock Text="开机运行后收纳到侧边栏" Style="{StaticResource SettingsTitleStyle}"/>
                    <TextBlock Text="启动后自动将工具栏折叠到屏幕边缘" Style="{StaticResource SettingsDescriptionStyle}"/>
                </StackPanel>
                <ui:ToggleSwitch Grid.Column="2" VerticalAlignment="Center"
                                 IsOn="{Binding Startup.IsFoldAtStartup, Mode=TwoWay}"/>
            </Grid>
        </Border>

        <!-- 工具栏设置分组 -->
        <TextBlock Text="工具栏" Style="{StaticResource SettingsGroupTitleStyle}"/>

        <!-- 浮动工具栏缩放 -->
        <Border Style="{StaticResource SettingsCardStyle}">
            <Grid>
                <Grid.ColumnDefinitions>
                    <ColumnDefinition Width="Auto"/>
                    <ColumnDefinition Width="*"/>
                    <ColumnDefinition Width="180"/>
                    <ColumnDefinition Width="60"/>
                </Grid.ColumnDefinitions>

                <!-- 图标容器 -->
                <Border Style="{StaticResource SettingsIconContainerStyle}">
                    <ui:FontIcon Glyph="&#xE740;" FontSize="18"
                                 Foreground="{StaticResource AccentBrush}"
                                 HorizontalAlignment="Center" VerticalAlignment="Center"/>
                </Border>

                <StackPanel Grid.Column="1" VerticalAlignment="Center" Margin="0,0,16,0">
                    <TextBlock Text="浮动工具栏缩放" Style="{StaticResource SettingsTitleStyle}"/>
                    <TextBlock Text="调整工具栏的显示大小" Style="{StaticResource SettingsDescriptionStyle}"/>
                </StackPanel>
                <Slider Grid.Column="2" VerticalAlignment="Center" Margin="0,0,12,0"
                        Minimum="0.5" Maximum="2.0"
                        Value="{Binding Appearance.ViewboxFloatingBarScaleTransformValue, Mode=TwoWay}"
                        TickFrequency="0.1" IsSnapToTickEnabled="True"
                        SmallChange="0.1" LargeChange="0.2"
                        Style="{StaticResource ModernSliderStyle}"/>
                <TextBlock Grid.Column="3" VerticalAlignment="Center" HorizontalAlignment="Right"
                           Text="{Binding Appearance.ViewboxFloatingBarScaleTransformValue, StringFormat={}{0:F1}x}"
                           Style="{StaticResource SettingsValueStyle}"/>
            </Grid>
        </Border>

        <!-- 浮动工具栏透明度 -->
        <Border Style="{StaticResource SettingsCardStyle}">
            <Grid>
                <Grid.ColumnDefinitions>
                    <ColumnDefinition Width="Auto"/>
                    <ColumnDefinition Width="*"/>
                    <ColumnDefinition Width="180"/>
                    <ColumnDefinition Width="60"/>
                </Grid.ColumnDefinitions>

                <!-- 图标容器 -->
                <Border Style="{StaticResource SettingsIconContainerStyle}">
                    <ui:FontIcon Glyph="&#xE7A6;" FontSize="18"
                                 Foreground="{StaticResource AccentBrush}"
                                 HorizontalAlignment="Center" VerticalAlignment="Center"/>
                </Border>

                <StackPanel Grid.Column="1" VerticalAlignment="Center" Margin="0,0,16,0">
                    <TextBlock Text="浮动工具栏透明度" Style="{StaticResource SettingsTitleStyle}"/>
                    <TextBlock Text="调整工具栏的不透明度" Style="{StaticResource SettingsDescriptionStyle}"/>
                </StackPanel>
                <Slider Grid.Column="2" VerticalAlignment="Center" Margin="0,0,12,0"
                        Minimum="0.1" Maximum="1.0"
                        Value="{Binding Appearance.ViewboxFloatingBarOpacityValue, Mode=TwoWay}"
                        TickFrequency="0.1" IsSnapToTickEnabled="True"
                        SmallChange="0.1" LargeChange="0.2"
                        Style="{StaticResource ModernSliderStyle}"/>
                <TextBlock Grid.Column="3" VerticalAlignment="Center" HorizontalAlignment="Right"
                           Text="{Binding Appearance.ViewboxFloatingBarOpacityValue, StringFormat={}{0:P0}}"
                           Style="{StaticResource SettingsValueStyle}"/>
            </Grid>
        </Border>

        <!-- 快速操作分组 -->
        <TextBlock Text="快速操作" Style="{StaticResource SettingsGroupTitleStyle}"/>

        <!-- 信息提示栏 -->
        <Border Style="{StaticResource InfoInfoBarStyle}">
            <Grid>
                <Grid.ColumnDefinitions>
                    <ColumnDefinition Width="Auto"/>
                    <ColumnDefinition Width="*"/>
                </Grid.ColumnDefinitions>
                <ui:FontIcon Glyph="&#xE946;" Foreground="{StaticResource InfoBrush}"
                             FontSize="20" VerticalAlignment="Top" Margin="0,2,16,0"/>
                <StackPanel Grid.Column="1">
                    <TextBlock Text="快捷键提示" Style="{StaticResource InfoBarTitleStyle}"
                               Foreground="{StaticResource InfoBrush}"/>
                    <TextBlock Style="{StaticResource InfoBarContentStyle}">
                        <Run Text="• Ctrl + S：切换收纳模式"/>
                        <LineBreak/>
                        <Run Text="• Ctrl + F：强制全屏化"/>
                        <LineBreak/>
                        <Run Text="• Ctrl + T：重置工具栏位置"/>
                        <LineBreak/>
                        <Run Text="• Ctrl + R：重启应用"/>
                    </TextBlock>
                </StackPanel>
            </Grid>
        </Border>

        <!-- 底部间距 -->
        <Border Height="40"/>
    </StackPanel>
</UserControl>
